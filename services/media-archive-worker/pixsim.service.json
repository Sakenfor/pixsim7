{
  "id": "media-archive-worker",
  "category": "services",
  "type": "worker",
  "name": "Worker (Media Archive)",
  "description": "Dedicated worker for slow media-archive jobs (bulk relocate/restore over S3/MinIO) — isolated from generation so long uploads don't eat its slots",
  "module": "arq",
  "args": ["pixsim7.backend.main.workers.arq_worker.MediaArchiveWorkerSettings"],
  "depends_on": ["db"],
  "auto_start": false,
  "enabled": true,
  "settings": [
    {
      "key": "max_jobs",
      "default": 1,
      "description": "Max concurrent relocation jobs (1 avoids parallel uploads saturating the link)"
    },
    {
      "key": "job_timeout",
      "default": 3600,
      "description": "Job timeout in seconds (the job self-re-enqueues ~40 min in, well before this)"
    }
  ]
}
