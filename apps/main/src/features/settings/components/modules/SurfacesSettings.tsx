/**
 * Surfaces Settings Module
 *
 * Registers the per-panel skin settings tab under the Appearance category.
 * The Appearance sidebar entry is already registered by IconSettings.
 */

import { registerSurfacesSettings } from '../../lib/schemas/surfaces.settings';

registerSurfacesSettings();
