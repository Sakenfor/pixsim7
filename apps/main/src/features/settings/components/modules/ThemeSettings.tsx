 
/**
 * Theme Settings Module
 *
 * Registers the theme settings schema under the Appearance category.
 * The Appearance sidebar entry is already registered by IconSettings.
 */

import { registerThemeSettings } from '../../lib/schemas/theme.settings';

registerThemeSettings();
