// Re-exported from shared UI. Import directly from '@pixsim7/shared.ui' in new code.
export { PanelHeader, type PanelHeaderProps } from '@pixsim7/shared.ui';

// Backwards-compatible alias for callers using PanelHeaderCategory as a type
import type { PanelCategory } from '@features/panels';
export type PanelHeaderCategory = PanelCategory;
