/**
 * Overlay Widget Settings Types — Re-exports
 *
 * Canonical source is @lib/widgets/overlayWidgetSettingsStore.
 * This file re-exports for convenience within the media component tree.
 */

export type {
  WidgetVisibilityMode,
  OverlayContextId,
  ConfigurableWidgetId,
  OverlayVisibilitySettings,
} from '@lib/widgets';

export {
  DEFAULT_OVERLAY_VISIBILITY,
  CONFIGURABLE_WIDGET_IDS,
  WIDGET_LABELS,
} from '@lib/widgets';
