import type { MediaOverlayComponentProps } from '../types';
import { PoseBoardOverlay } from '@features/poseBoard';

export function PoseBoardOverlayMain(_props: MediaOverlayComponentProps) {
  return <PoseBoardOverlay />;
}
