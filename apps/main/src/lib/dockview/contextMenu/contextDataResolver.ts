/**
 * Context Data Resolver - Re-exported from shared package
 */
export {
  contextDataRegistry,
  contextDataCache,
  contextMenuAttrs,
  contextMenuIgnore,
  extractContextFromElement,
  useRegisterContextData,
  useContextMenuItem,
  type ContextDataResolver,
  type ContextMenuAttrs,
} from '@pixsim7/shared.ui.context-menu';
