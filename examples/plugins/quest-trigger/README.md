# Quest Trigger Plugin

Generated by `create-pixsim-plugin`

## Overview

Quest trigger renderer with async loading

## Plugin Type

**renderer**

## Installation

### 1. Register the Plugin

```typescript
// For interaction plugins:
import { questTriggerPlugin } from './plugins/quest-trigger';
import { interactionRegistry } from '@pixsim7/types';

interactionRegistry.register(questTriggerPlugin);

// For node type plugins:
import { questTriggerNodeType } from './plugins/quest-trigger';
import { nodeTypeRegistry } from '@pixsim7/types';

nodeTypeRegistry.register(questTriggerNodeType);

// For renderer plugins:
import { questTriggerRenderer } from './plugins/quest-trigger';
import { nodeRendererRegistry } from '@/lib/graph/nodeRendererRegistry';

nodeRendererRegistry.register(questTriggerRenderer);

// For helper plugins:
import { QuestTriggerHelper } from './plugins/quest-trigger';
// Use the helper methods directly in your code
```

### 2. Configure (if applicable)

Update your configuration to enable this plugin:

```typescript
const config = {
  quest-trigger: {
    enabled: true,
    // Add plugin-specific config
  },
};
```

## Usage






### Rendering Nodes

The renderer automatically applies when nodes of type `quest-trigger` are displayed in the graph view.

Customize the renderer component in `QuestTriggerRenderer.tsx`.




## Development

### Customization

Edit the generated files to implement your plugin logic:

- `quest-trigger.ts` - Main plugin implementation
- `README.md` - This documentation

### Testing

Add tests for your plugin:

```typescript
describe('Quest Trigger Plugin', () => {
  it('should execute successfully', async () => {
    const result = await questTriggerPlugin.execute(config, context);
    expect(result.success).toBe(true);
  });
});
```

## Configuration

| Option | Type | Default | Description |
|--------|------|---------|-------------|
| `enabled` | boolean | `true` | Enable/disable this plugin |

## Examples

Add usage examples here...

## API Reference

### Configuration Interface

```typescript
interface QuestTriggerConfig {
  enabled: boolean;
  // Add your config fields
}
```

### Methods

Document your plugin's public methods here...

## Troubleshooting

### Common Issues

1. **Plugin not registering**: Ensure you've imported and registered the plugin at app initialization.
2. **Type errors**: Check that your data structures match the expected interfaces.

## License

Same as parent project.
