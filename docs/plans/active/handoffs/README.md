# Active Plan Handoffs

This folder stores prompt-only handoff docs for agent execution.

- Keep canonical implementation plans in `docs/plans/active/`.
- Keep handoff prompts here to avoid mixing executable plans with copy/paste agent prompts.
- Handoff files are not registered in `docs/plans/registry.yaml`.
