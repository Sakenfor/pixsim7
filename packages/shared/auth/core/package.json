{
  "name": "@pixsim7/shared.auth.core",
  "version": "0.0.1",
  "private": true,
  "description": "Shared authentication logic for PixSim7 (login, logout, token management)",
  "main": "src/index.ts",
  "module": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "require": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "files": ["src", "dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "zustand": "^4.0.0 || ^5.0.0"
  },
  "dependencies": {
    "@pixsim7/shared.api.client": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "zustand": "^5.0.0"
  }
}
