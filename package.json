{
  "name": "@pixsim7/workspace",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "frontend",
    "game-frontend",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r exec tsc --noEmit",
    "lint": "echo 'add lint script per package'",
    "generate-docs": "tsx packages/game-core/src/utils/generatePluginDocs.ts"
  },
  "devDependencies": {
    "tsx": "^4.20.6"
  }
}
